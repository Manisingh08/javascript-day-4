var password = prompt("Enter in the password");

for (password >=100; password < 100; password++) {
   
    if (password >= 100) {
        document.getElementById("demo").innerHTML = "correct";      }
      else 
      {
        document.getElementById("demo").innerHTML = "retry";      }
    }

